export enum OrderStatus {
    Pending = "pending",
    Paid = "paid",
    Processing = "processing",
    Shipped = "shipped",
    Cancelled = "cancelled",
    Finished = "finished",
}

export enum DeliveryMethod {
    Delivery = "delivery",
    Pickup = "pickup",
}
